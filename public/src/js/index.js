// Logic for typing animation on index page
const typeOptions = {
  stringsElement: "#typed-strings",
  typeSpeed: 50,
  loop: false,
  showCursor: false,
  backDelay: 850,
  backSpeed: 30,
}

const typed = new Typed("#js-animated-type", typeOptions)
